# Vite автоматически загружает этот файл в режиме разработки (npm run dev)
# Он переопределяет значения из .env для локальной разработки

# Локальный бэкенд вместо продакшен-сервера
VITE_API_URL=http://localhost:3001
